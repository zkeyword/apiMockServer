<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %>
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    item.ast.resourceGroups[0].resources[0]

    <% body.forEach(item=>{ %>
        <p><%= item.name %></p>
        <p><%= item.description %></p>
        <%= item.resourceGroups %>

    <% }) %>
</body>

</html>